@using hoyu_scripts
@fast
@if gate_history.length() == 0
        @var gate_history.append(14)
@else
        @var gate_history.reverse()
        @if gate_history[0] != 14
                @var gate_history.reverse()
                @var gate_history.append(14)
        @else
                @var gate_history.reverse()
        @fi
@fi

# https://paste.minr.org/yapovepipo