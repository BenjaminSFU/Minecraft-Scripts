@using hoyu_scripts
@fast
@if gate_history.length() == 0
        @var gate_history.append(8)
@else
        @var gate_history.reverse()
        @if gate_history[0] != 8
                @var gate_history.reverse()
                @var gate_history.append(8)
        @else
                @var gate_history.reverse()
        @fi
@fi

# https://paste.minr.org/powosezoyo